powershell -ExecutionPolicy Bypass -File ./watch.ps1

# TicTacToe – React + .NET

A simple TicTacToe game where you play against a CPU opponent.  
Built using **React (Vite)** for the frontend and **.NET** for the backend move computation.

---

## 🚀 Features
- Play against a CPU with server-computed moves
- Lightweight React UI
- Clean .NET Web API for game logic
- Easy to run and extend

---

## 🛠️ Tech Stack
### Frontend
- React (Vite)
- TypeScript

### Backend
- .NET 8 Web API

---

## 📦 Installation & Setup

1. Clone the repository:
   ```bash
   git clone <your-repo-url>
   cd <project-folder>
2. Navigate to the frontend folder:
    ```bash
    cd Frontend
    npm install
3. Running the backend app (Development Mode)
   The app will start locally
    (usually http://localhost:5101).
4. Running the frontend app 
    ```bash
    npm dev
    

